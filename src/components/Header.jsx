import { Link } from 'react-router-dom';
import Navbar from './UI/Navbar';

export default function Header() {
  return (
    <nav> Frank Romero
      <div>
        <Navbar/>
      </div>
    </nav>
  );
}
